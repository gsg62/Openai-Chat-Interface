import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  baseContainer: {
    backgroundColor: "#fff",
    alignItems: "center",
    padding: 10,
    height: "100%",
  },
  titleText: {
    marginTop: 40,
    fontSize: 24,
    padding: 20,
  },
  indicator: {
    top: 20,
  },
});

export default styles;
